<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.btl">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PackageCreation"></activity>
        <activity android:name=".UserInformation" />
        <activity android:name=".Login" />
        <activity
            android:name=".ShopNavigatorMenu"
            android:label="@string/title_activity_shop_navigator_menu" />
        <activity
            android:name=".ShipperNavigatorMenu"
            android:label="@string/title_activity_shipper_navigator_menu" />
        <activity android:name=".Registration" />
        <activity android:name=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>